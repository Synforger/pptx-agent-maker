# Roadmap — pptx-agent-maker

> 個人プロジェクトです。 「今安定して使えるのか / 何が作りかけか」 を判断するための一覧。

## いま使えること

- **頁を宣言で組む** — 頁を割る / 収める / 縦横比を保つ。座標を受け取る口は無い
- **テンプレートの複製と過去デッキからの輸入** — 媒体ごと運び、読み位置で対応づける
- **案件ごとの見た目** — 書体と色を `[theme]` で差し替える (= 頁の割り方は動かない)
- **manifest 1 枚から 1 デッキ** — 並び順の真値は manifest だけ
- **焼いた後の検査 6 口** — 頁の外 / 文字の下限 / 空セル / 内部 file 名 / テンプレートの語の残り
- **手編集の保護** — 人が直したデッキを黙って上書きせず、退避して差分を語で出す
- **ライブプレビュー** — 組むのと同じコマンドで、焼いたものをブラウザで追う
- **配る形** — wheel と source archive の両方を出す (= プロジェクトテンプレートはツールと一緒に配られる)
- **重なりを見る** — 描かれた文字と、頁が乗るレイアウトの実体を比べる
  (= 枠どうしではない。枠で見ると画面では離れている物が上がる)

## 今後やる予定 (= committed)

- 使える型を増やす (= 現在は 見出し / 帯 / カード / 図 / 表 / 読み方 / 出所)。
  **足すのは実物の頁がそれを要求したとき** ― 先に増やすと、使われない型が残る

## 検討中 (= not committed)

- テンプレートを「何回使われたか」で自動的に型へ上げる
- 宣言した頁の差分をプレビュー側で並べて見る

## やらないと決めたこと

- **座標を受け取る API** — 頁の外に出る頁と重なる兄弟は、割ることしかできない構造で防ぐ
- **案件固有の頁** — 何をどう並べるかは案件のもので、この repo は使える型までを持つ
